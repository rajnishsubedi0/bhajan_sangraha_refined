<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_app"
    tools:context=".Favourites.FavouriteBookmarked">

    <!-- Custom Top App Bar Container optimized for Favourites view -->
    <FrameLayout
        android:id="@+id/app_bar_container"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/bg_list_item"
        android:elevation="4dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <RelativeLayout
            android:id="@+id/header_normal_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="visible">

            <!-- Back Arrow Navigation Button -->
            <ImageView
                android:id="@+id/btn_back_favourite"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:padding="12dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:focusable="true"
                android:src="@drawable/ic_back_arrow"
                android:tint="@color/text_primary"
                android:contentDescription="Back Icon" />

            <!-- "Favourites" Title -->
            <TextView
                android:id="@+id/header_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toEndOf="@id/btn_back_favourite"
                android:layout_centerVertical="true"
                android:layout_marginStart="8dp"
                android:text="@string/title_favourites"
                android:textColor="@color/text_primary"
                android:textSize="26sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="-0.01" />

            <!-- Dummy Favorite Icon with visibility GONE (keeps backward compatibility and null safety) -->
            <ImageView
                android:id="@+id/btn_favourite_header"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone"
                tools:ignore="ContentDescription" />

        </RelativeLayout>

    </FrameLayout>

    <!-- List of Favorite Bhajans -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bg_app"
        app:fastScrollEnabled="true"
        app:fastScrollVerticalThumbDrawable="@drawable/fastscroll_thumb_vertical"
        app:fastScrollVerticalTrackDrawable="@drawable/fastscroll_track_vertical"
        app:fastScrollHorizontalThumbDrawable="@drawable/fastscroll_thumb_horizontal"
        app:fastScrollHorizontalTrackDrawable="@drawable/fastscroll_track_horizontal" />

</LinearLayout>
